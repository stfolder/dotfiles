# Keep the frame minimal while preserving normal resize behavior.
hide_window_decorations titlebar-only
macos_titlebar_color background

# Left Option is Alt for terminal shortcuts; Right Option keeps macOS characters.
macos_option_as_alt left

# The approved Nyx translucency treatment.
background_blur 24
